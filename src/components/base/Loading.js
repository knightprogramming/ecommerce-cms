import React from "react";
import { FontAwesomeIcon } from "@fortawesome/react-fontawesome";

const Loading = () => (
  <div className="loading">
    <FontAwesomeIcon className="loading__icon" icon="spinner" />
  </div>
);

export default Loading;
